---
// @ts-nocheck
import Layout from "../../layouts/Layout.astro";

export async function getStaticPaths() {
  const url = "https://gthqpnquordprfbbxrrc.supabase.co/rest/v1/ShopCard?select=*";
  const key = "sb_publishable_0kC09y0_QNUGVyZSy7xOWQ_itr3lxQ_";

  const options = {
    headers: {
      apikey: key,
      Authorization: `Bearer ${key}`,
    },
  };

  const res = await fetch(url, options);
  const products = await res.json();

  return products.map((product) => ({
    params: { id: product.id.toString() },
    props: { product },
  }));
}

const { product } = Astro.props;

const descriptionLines = product.description
  ? product.description
      .split("\n")
      .map((line) => line.trim())
      .filter(Boolean)
  : [];

const descriptionText = descriptionLines.filter((line) => !line.startsWith("–") && !line.startsWith("-")).join(" ");

const descriptionPoints = descriptionLines.filter((line) => line.startsWith("–") || line.startsWith("-")).map((line) => line.replace(/^[-–]\s*/, ""));
---

<Layout navVariant="light" pageColor="orange" footerVariant="orange">
  <!-- produkt detaljeside -->
  <section class="product-page">
    <!-- tilbage pil -->
    <a href="/shop" class="back-button" aria-label="Tilbage"></a>

    <!-- produkt layout -->
    <div class="product-layout">
      <!-- produkt billede område -->
      <div class="product-image-wrap">
        <!-- forrige billede pil -->
        <button class="image-arrow image-arrow-left" type="button" aria-label="Forrige billede"></button>

        <!-- produkt billede -->
        <img id="product-image" src={product.image_url} data-image-one={product.image_url} data-image-two={product.image2_url} alt={`Billede af ${product.name}`} />

        <!-- næste billede pil -->
        <button class="image-arrow image-arrow-right" type="button" aria-label="Næste billede"></button>
      </div>

      <!-- produkt information -->
      <div class="product-info">
        <!-- øverste produkt info -->
        <div class="product-top">
          <div>
            <h2>{product.name}</h2>
            <p class="product-category">{product.category}</p>
          </div>

          <!-- køb område -->
          <div class="product-buy">
            <p class="product-price">KR. {product.price}</p>
            <a href="#" class="add-to-cart site-hover">Tilføj til kurv</a>
          </div>
        </div>

        <!-- produkt beskrivelse -->
        <div class="product-description">
          <h3>Om produktet</h3>

          {descriptionText && <p>{descriptionText}</p>}

          {
            descriptionPoints.length > 0 && (
              <ul>
                {descriptionPoints.map((point) => (
                  <li>{point}</li>
                ))}
              </ul>
            )
          }
        </div>
      </div>
    </div>
  </section>
</Layout>

<style>
  /* produkt detaljeside */
  .product-page {
    color: var(--white);
    padding-bottom: 5rem;
  }

  /* pil knap*/
  .back-button,
  .image-arrow {
    width: 1.5rem;
    height: 1.5rem;
  }

  /* pil form */
  .back-button::before,
  .image-arrow::before {
    content: "";
    display: block;
    width: 1rem;
    height: 1rem;
    border-left: 0.18rem solid var(--white);
    border-bottom: 0.18rem solid var(--white);
    border-radius: 0.12rem;
  }

  /* venstre pil */
  .back-button::before,
  .image-arrow-left::before {
    transform: rotate(45deg);
  }

  /* højre pil */
  .image-arrow-right::before {
    transform: rotate(225deg);
  }

  /* hover på pil*/
  .back-button:hover::before,
  .image-arrow:hover::before {
    border-left-color: var(--citat);
    border-bottom-color: var(--citat);
  }
  /* hover på pil*/

  /* produkt layout */
  .product-layout {
    display: flex;
    flex-direction: column;
  }

  /* produkt billede område */
  .product-image-wrap {
    position: relative;
    margin-left: -1.5rem;
    margin-right: -1.5rem;
  }

  /* produkt billede */
  .product-image-wrap img {
    display: block;
    width: 100%;
    aspect-ratio: 1 / 1;
    object-fit: contain;
  }

  /* billede pile */
  .image-arrow {
    position: absolute;
    top: 50%;
    transform: translateY(-50%);
    width: 2rem;
    height: 2rem;
    border: 0;
    background: transparent;
    cursor: pointer;
  }

  /* venstre billede pil */
  .image-arrow-left {
    left: 1rem;
  }

  /* højre billede pil */
  .image-arrow-right {
    right: 1rem;
  }

  /* produkt information */
  .product-info {
    padding-top: 1rem;
  }

  /* øverste produkt info */
  .product-top {
    display: flex;
    justify-content: space-between;
    gap: 2rem;
    margin-bottom: 3rem;
  }

  /* produkt titel */
  .product-info h2 {
    color: var(--white);
    font-size: 1.2rem;
  }

  /* produkt kategori */
  .product-category {
    margin: 0.5rem 0 0;
    opacity: 70%;
  }

  /* prik før kategori */
  .product-category::before {
    content: "";
    display: inline-block;
    width: 0.45rem;
    height: 0.45rem;
    margin-right: 0.2rem;
    border-radius: 50%;
    background: var(--white);
  }

  /* køb område */
  .product-buy {
    flex: 0 0 auto;
    text-align: right;
  }

  /* produkt pris */
  .product-price {
    white-space: nowrap;
  }

  /* køb link */
  .product-buy a {
    color: var(--white);
    text-transform: uppercase;
    text-decoration: underline;
    white-space: nowrap;
  }

  /* produkt tekst */
  .product-description h3,
  .product-description p,
  .product-description li,
  .product-category,
  .product-price,
  .product-buy a {
    font-family: "Unica", sans-serif;
    color: var(--white);
  }

  /* produkt beskrivelse overskrift */
  .product-description h3 {
    margin: 0 0 0.25rem;
    font-size: 0.9rem;
    line-height: 1.2rem;
  }

  /* produkt beskrivelse tekst */
  .product-description p {
    margin: 0;
    font-size: 0.9rem;
    line-height: 1.35rem;
  }

  /* produkt beskrivelse liste */
  .product-description ul {
    margin: 1rem 0 0;
    padding: 0;
    list-style: none;
  }

  /* produkt beskrivelse listepunkt */
  .product-description li {
    margin: 0;
    font-family: "Unica", sans-serif;
    color: var(--white);
    font-size: 0.9rem;
    line-height: 1.35rem;
  }

  /* bindestreg før listepunkt */
  .product-description li::before {
    content: "– ";
  }

  /* tablet billede placering */
  @media (min-width: 600px) {
    .product-image-wrap {
      margin-left: -2rem;
      margin-right: -2rem;
      transform: translateY(-4rem);
    }
  }

  /* desktop layout */
  @media (min-width: 900px) {
    .product-page {
      min-height: 70svh;
    }

    .back-button {
      position: relative;
      z-index: 20;
      margin-bottom: 3rem;
    }

    .product-layout {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 5rem;
      align-items: start;
    }

    .product-image-wrap {
      margin-left: 0;
      margin-right: 0;
    }

    .product-info {
      padding-top: 3rem;
      max-width: 30rem;
    }

    .product-info h2 {
      font-size: 1.7rem;
      margin-bottom: -0.5rem;
    }

    .product-top {
      display: grid;
      grid-template-columns: 1fr auto;
      column-gap: 2rem;
      align-items: start;
      margin-bottom: 3rem;
    }

    .product-buy {
      display: contents;
    }

    .product-price {
      grid-column: 2;
      grid-row: 1;
      margin: 0.35rem 0 0;
      font-size: 1rem;
      text-align: right;
    }

    .product-buy a {
      grid-column: 1;
      grid-row: 2;
      width: fit-content;
    }
  }

  /* hover på tilføj til kurv */
  .product-buy a.add-to-cart {
    transition: color 0.2s ease;
  }

  .product-buy a.add-to-cart:hover {
    color: var(--citat);
  }
</style>

<script>
  /* gemmer produkt billede og pile i variabler */
  const productImage = document.querySelector("#product-image");
  const leftArrow = document.querySelector(".image-arrow-left");
  const rightArrow = document.querySelector(".image-arrow-right");

  /* tjekker at billede og pile findes */
  if (productImage && leftArrow && rightArrow) {
    const images = [productImage.dataset.imageOne, productImage.dataset.imageTwo].filter(Boolean);

    let currentImage = 0;

    /* skifter mellem produkt billeder */
    function changeImage(direction) {
      currentImage = (currentImage + direction + images.length) % images.length;
      productImage.src = images[currentImage];
    }

    /* klik på venstre pil */
    leftArrow.addEventListener("click", () => changeImage(-1));

    /* klik på højre pil */
    rightArrow.addEventListener("click", () => changeImage(1));
  }
</script>
