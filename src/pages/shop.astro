---
import Layout from "../layouts/Layout.astro";
import H2 from "../components/H2.astro";
import ShopCard from "../components/ShopCard.astro";

const url = "https://gthqpnquordprfbbxrrc.supabase.co/rest/v1/ShopCard?select=*&order=id.asc";
const key = "sb_publishable_0kC09y0_QNUGVyZSy7xOWQ_itr3lxQ_";

const options = {
  headers: {
    apikey: key,
    Authorization: `Bearer ${key}`,
  },
};

const res = await fetch(url, options);
const products = await res.json();
---

<Layout navVariant="light" pageColor="orange" footerVariant="orange">
  <section class="shop-hero">
    <H2 title="Shop" />
  </section>

  <section class="shop-grid">
    {products.map((product) => <ShopCard data={product} />)}
  </section>

  <section class="image-row">
    <img src="/dittehoodie.webp" alt="Ditte med hoodie" />
    <img src="/cap2.webp" alt="Cap på deltager" />
  </section>
</Layout>

<style>
  .shop-hero :global(h2) {
    color: var(--white);
  }

  .shop-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 3rem 3rem;
    margin-bottom: 2rem;
  }

  .image-row {
    margin-left: -1.5rem;
    margin-right: -1.5rem;
  }

  .image-row img {
    width: 100%;
    height: auto;
    display: block;
  }

  @media (min-width: 900px) {
    .shop-hero {
      margin-bottom: 2rem;
    }

    .shop-grid {
      grid-template-columns: repeat(4, 1fr);
      gap: 4rem 5rem;
      margin-bottom: 8rem;
    }

    .image-row {
      display: grid;
      grid-template-columns: 1fr 1fr;
      margin-left: -2rem;
      margin-right: -2rem;
      margin-bottom: 5rem;
    }

    .image-row img {
      height: 34rem;
      object-fit: cover;
    }
  }
</style>
