---

---

<section class="hero-wrapper">
  <video class="hero-video" src="herovideo_bbw.mp4" autoplay muted loop playsinline></video>

  <button class="sound-toggle" type="button" aria-label="Slå lyd til" aria-pressed="false">
    <span class="sound-icon sound-icon-muted" aria-hidden="true">
      <svg viewBox="0 0 24 24">
        <path d="M11 5 6 9H3v6h3l5 4V5Z"></path>
        <path d="M17 9l4 4"></path>
        <path d="M21 9l-4 4"></path>
      </svg>
    </span>

    <span class="sound-icon sound-icon-on" aria-hidden="true">
      <svg viewBox="0 0 24 24">
        <path d="M11 5 6 9H3v6h3l5 4V5Z"></path>
        <path d="M15.5 8.5a5 5 0 0 1 0 7"></path>
        <path d="M18.5 5.5a9 9 0 0 1 0 13"></path>
      </svg>
    </span>
  </button>

  <div class="hero-text">
    <h1>busy being well</h1>
  </div>
</section>

<script>
  const video = document.querySelector(".hero-video");
  const soundButton = document.querySelector(".sound-toggle");

  if (video && soundButton) {
    soundButton.addEventListener("click", async () => {
      const shouldUnmute = video.muted;

      video.muted = !shouldUnmute;
      video.volume = shouldUnmute ? 1 : 0;

      soundButton.classList.toggle("is-on", shouldUnmute);
      soundButton.setAttribute("aria-label", shouldUnmute ? "Slå lyd fra" : "Slå lyd til");
      soundButton.setAttribute("aria-pressed", String(shouldUnmute));

      if (shouldUnmute) {
        await video.play().catch(() => {});
      }
    });
  }
</script>

<style>
  .hero-wrapper {
    position: relative;
    width: 100%;
    height: 100vh;
    overflow: hidden;
    margin-bottom: 3rem;
  }

  .hero-video {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    z-index: 1;
  }

  .hero-text {
    position: absolute;
    z-index: 1;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    text-align: center;
    color: var(--cream);
  }
  .hero-text h1 {
    color: var(--cream);
    font-size: 2.9rem;
    font-weight: 150;
    margin: 0;
  }

  @media (min-width: 600px) {
    .hero-text h1 {
      font-size: clamp(3rem, 10vw, 6rem);
    }

    .hero-wrapper {
      position: relative;
      width: 100%;
      height: 100vh;
      overflow: hidden;
      margin-bottom: 0rem;
    }
  }

  .sound-toggle {
    position: absolute;
    right: 1rem;
    bottom: 1rem;
    z-index: 20;
    width: 2.8rem;
    height: 2.8rem;
    display: grid;
    place-items: center;
    border: none;
    background: transparent;
    color: var(--cream);
    cursor: pointer;
    padding: 0;
  }

  .sound-icon {
    width: 2.1rem;
    height: 2.1rem;
  }

  .sound-toggle:hover {
    color: var(--citat);
  }

  .sound-icon svg {
    width: 100%;
    height: 100%;
    fill: none;
    stroke: currentColor;
    stroke-width: 1.8;
    stroke-linecap: round;
    stroke-linejoin: round;
  }

  .sound-icon-on {
    display: none;
  }

  .sound-toggle.is-on .sound-icon-muted {
    display: none;
  }

  .sound-toggle.is-on .sound-icon-on {
    display: block;
  }

  @media (min-width: 600px) {
    .sound-toggle {
      right: 2rem;
      bottom: 2rem;
    }
  }
</style>
